%li.itirann-imag
  .itirann-styl
    = link_to product_path(product.id) do
      .style_card
        .style-card__1
          %span.style-card__text 
            = product.name
        .style_thumbnail__N_xAi
          %span.label
            ¥
            = product.price
          = image_tag "#{product.images.first.url}", class: "images"
          
                  
          

